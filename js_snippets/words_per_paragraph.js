const paragraphs = [...document.querySelectorAll(\'p\')];\r\n\r\nif (!paragraphs.length) {\r\n    return seoSpider.data(0);\r\n}\r\n\r\nconst totalWords = paragraphs.reduce((sum, p) => {\r\n    const words = p.innerText\r\n        .trim()\r\n        .split(\/\\s+\/)\r\n        .filter(Boolean);\r\n\r\n    return sum + words.length;\r\n}, 0);\r\n\r\nconst avgWords = totalWords \/ paragraphs.length;\r\n\r\nreturn seoSpider.data(avgWords.toFixed(2));
